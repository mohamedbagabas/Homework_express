export interface People {
    id: string;
    name: string;
  }
  
  export interface Grade {
    id: string;
    grade: string;
  }
  
  export interface Task {
    id: string;
    title: string;
    description: string;
    status: string;
  }